<c:FragmentDefinition
    xmlns="sap.m"
    xmlns:c="sap.ui.core"
>
    <OverflowToolbar>
       
        <!-- <Button
            icon="sap-icon://alert"
            text="{=${message>/}.length}"
            visible="{=${message>/}.length > 0}"
            type="Emphasized"
            press="onMessagePopoverPress"
        /> -->

       <Button
            type="Emphasized"
            text="{i18n>stockQuery}"
            press="onStockQuery"
        /> 
        <ToolbarSpacer />
        <Button text="{i18n>clear}" press="onClear" tooltip="{i18n>clear}" type="Reject">
				
			</Button>
                    <ToolbarSpacer />
        <Button
            
            press="onSave"
            icon="sap-icon://save"
            tooltip="{i18n>save}"
            type="Accept"
        />
    </OverflowToolbar>
</c:FragmentDefinition>